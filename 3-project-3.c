#include<stdio.h>

main()

{
	int num,fd,ld,sum;
	printf("Enter a number: ");
	scanf("%d",&number);
	id=number%10;
	fd=number;
	
	while(fd>=10)
	{
		fd/=10
	}
	sum=fd+id;
	
	printf("The sum of the first and last digits is:%d\n",sum;)
}